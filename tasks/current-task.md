# Current Task

## HUMAN REVIEW COMPLETE — TFL-ORG-RELREP-001 v1.0

Outcome: `REPRESENTATION_INSUFFICIENT`.

Review record:

- `experiments/TFL-ORG-RELREP-001/v1.0/HUMAN_REVIEW.md`

Reason:

The frozen canonical `R_id` quotients all node permutations before the project has established which permutations are genuine organizational symmetries. As a result, a change in stable endpoint/coupling assignment can be mapped to the same canonical structural representation. The v1.0 representation therefore cannot simultaneously answer both:

- `who is coupled to whom` in stable source identity;
- structural equivalence modulo interchangeable nodes.

The review preserves all predecessor records unchanged and does not reject the broader relational-organization hypothesis.

## NEXT RESEARCH QUESTION — not yet authorized for execution

Before another exploratory run, define the organizational symmetry relation explicitly:

**Which permutations are organization-preserving symmetries, and which permutations change organizational state?**

A follow-up representation should separate at least:

- `R_labeled` — stable endpoint/coupling identity;
- `R_struct` — structure modulo explicitly permitted permutations;
- optional `R_role` — role equivalence only if independently defined;
- `P` — operator summary derived from one named relational layer.

Required information-loss maps should be distinguished explicitly:

- `R_labeled -> R_struct`;
- `R_struct -> P`;
- `R_labeled -> P`.

Do not start `TFL-RELSTATE-001`, introduce a new operator, or execute a new experiment until a new protocol is explicitly authorized.
